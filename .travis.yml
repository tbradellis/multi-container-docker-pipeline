sudo: required
services:
  - docker


before_install:
  - docker build -t bellisbkb/test-react -f ./client/Dockerfile-dev .

script:
  - docker run bellisbkb/test-react npm run test -- --coverage

after_success: 
  - docker built -t bellisbkb/multi-client ./client   
  - docker built -t bellisbkb/multi-server ./server
  - docker built -t bellisbkb/multi-nginx ./nginx 
  - docker built -t bellisbkb/multi-worker ./worker
  #overcome command line sequential login prompt 
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push bellisbkb/multi-client
  - docker push bellisbkb/multi-server
  - docker push bellisbkb/multi-nginx
  - docker push bellisbkb/multi-worker



# deploy:
#   provider: elasticbeanstalk
#   region: "us-west-2"
#   app: "multi-container-docker-pipeline"
#   env: "DockerReactRedis-env"
#   bucket_name: "elasticbeanstalk-us-west-2-374863017875"
#   bucket_path: "docker-react"
#   on:
#     branch: master
#   access_key_id: "$AWS_ACCESS_KEY"
#   secret_access_key:
#       secure: "$AWS_SECRET_KEY"
